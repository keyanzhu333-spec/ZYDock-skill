interface:
  display_name: "ZYDock"
  short_description: "Cloud DiffDock docking via NVIDIA NIM"
  default_prompt: "Use $zydock to dock a ligand against a protein and analyze the predicted binding poses."

policy:
  allow_implicit_invocation: true
